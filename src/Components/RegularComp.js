import React from "react";

const RegularComp = (props) => {
  console.log("********************RegularComp ***********");

  return <div>from regular comp {props.name}</div>;
};

export default RegularComp;
